# ChainCloud AI 简历面试追问题库

> 用途：围绕简历中的四项项目经历，模拟面试官从业务理解、架构设计、代码实现、故障边界到指标可信度的逐层追问。  
> 使用建议：每个问题先尝试在 30 秒内回答，再展开到 2 分钟；回答尽量遵循“问题背景 → 方案选择 → 关键实现 → 结果 → 边界与改进”。  
> 审阅口径：问题和回答提示以当前仓库代码及现有评测报告为依据，不把设计文档中的目标自动视为已上线效果。

## 一、项目总览与开场题

### 1. 请用一分钟介绍这个项目

**面试官可能追问**

1. 这个项目解决的是谁的什么问题？
2. 为什么需要 Agent，而不是普通查询平台或固定工作流？
3. 用户输入一个问题后，系统完整链路是什么？
4. 你在项目中具体负责哪些模块？哪些不是你做的？
5. 项目最难的三个问题是什么？
6. 项目目前处于 Demo、内部试用还是生产阶段？
7. 你认为项目最能体现工程能力的部分是什么？

**回答主线**

项目面向链上数据分析、运营和风控场景，把 PostgreSQL、ClickHouse、TRON/Ethereum RPC、图表、调度和监控能力统一为自然语言入口。请求进入 FastAPI 后，由 LangGraph 在 Direct 和 Planned 两条路径间路由；复杂任务经过规划、权限检查、参数校验、工具执行、结果评估、重试或重规划，最后基于工具证据生成回答。后台还通过 APScheduler 增量扫描交易并发送飞书通知。

**容易被追穿的点**

- 不要把数据库中的 JustLend、跨链业务数据说成“完整链上数据”。
- 不要笼统说“已经生产落地”，除非能说明部署规模、运行时间、用户量和线上监控数据。
- 清楚区分：Agent Checkpoint、长期 Memory、上下文摘要、工具原始结果存储。

### 2. 为什么使用 LangGraph？

**由浅入深追问**

1. LangGraph 相比直接调用大模型加 tools，解决了什么问题？
2. 图中有哪些核心节点和条件边？
3. AgentState 保存了什么？为什么需要显式状态？
4. 为什么不用 LangChain AgentExecutor？
5. 哪些决策由模型完成，哪些必须由确定性代码完成？
6. Checkpoint 在审批、澄清和故障恢复中如何工作？
7. 图结构变更后，旧 Checkpoint 如何兼容？
8. 同一个 `thread_id` 并发请求会发生什么？

**回答要点**

- 多步骤任务不是一次 Tool Calling，而是有状态的计划、执行、验证和恢复流程。
- LangGraph 适合表达 Direct/Planned 分支、权限中断、补参恢复、retry/replan/partial/fail 等状态迁移。
- 权限、预算、工具存在性和必要参数检查应尽量使用确定性代码；模型负责模糊意图理解、规划和语义质量判断。
- PostgreSQL Checkpointer 可跨请求、跨重启恢复；内存 Checkpointer 重启即丢失。
- 当前同线程并发写需要谨慎，不能声称已经实现严格的并发串行化。

---

## 二、Agent 执行链路建设

简历原文：

> 基于 LangGraph 搭建链上数据分析 Agent，支持数据库、链上节点、可视化等多类工具能力；针对简单查询和复杂任务设计不同执行流程，完善工具异常恢复链路，对瞬时故障自动重试，持续失败后支持参数修正、备用工具切换及任务重规划或降级返回，提升多工具任务执行稳定性。

### 第一层：功能和架构理解

1. Agent 当前支持哪些工具？分别解决什么问题？
2. PostgreSQL、ClickHouse 和链上 RPC 的数据边界有什么区别？
3. 可视化工具输出的是什么？前端如何展示？
4. 一条“查数据库后用交易哈希核验链上状态”的请求如何执行？
5. 工具是如何注册和暴露给模型的？
6. 如何避免模型调用不存在的工具？
7. 多工具结果如何传递给下一步？
8. 为什么不能把所有工具描述一次性塞给每个节点？

**回答要点**

- 数据库工具适合业务数据查询，RPC 用于链上事实核验，二者不能相互替代。
- 工具通过 Registry 统一装配；Planner Validator 和执行前校验共同阻止非法工具进入执行。
- 上一步结果会压缩成结构化 facts，并保留原始结果引用；例如从数据库结果提取 tx hash，供 RPC 步骤绑定参数。
- 图表使用 pandas/Plotly 生成 HTML，由 FastAPI 静态目录提供访问。

### 第二层：Direct 与 Planned 双路径

1. 什么请求走 Direct，什么请求走 Planned？
2. 为什么简单任务不统一走 Planned？
3. 路由是纯规则、纯 LLM，还是混合策略？
4. API 强制模式与自动路由冲突时谁优先？
5. 路由模型失败或低置信度时如何处理？
6. Direct 路径是否也能连续调用多个工具？
7. Direct 路径如何控制工具调用次数？
8. Planned 路径的 PlanStep 包含哪些信息？
9. Planner 输出非法 JSON 或非法依赖怎么办？
10. Planned 比 Direct 多了哪些模型调用，成本增加多少？
11. 如何评估路由是否真的带来收益，而不是增加复杂度？

**推荐回答框架**

- API 显式模式优先，其次确定性规则，模糊请求再交给模型分类。
- 简单解释或单工具任务走 Direct，减少 Planner、Evaluator 等额外模型调用；存在依赖、多步骤、副作用或高风险时走 Planned。
- 低置信度或路由失败时保守进入 Planned。
- Planned 中 PlanStep 至少应能表达目标、成功标准、依赖、候选工具、fallback 和权限要求。
- Planner 输出先做结构校验；重试仍失败时降级为安全单步骤计划。

### 第三层：执行和结果判断

1. 工具 HTTP/SQL 调用成功，为什么不等于任务成功？
2. 每一步的成功标准从哪里来？
3. Evaluator 如何决定 `pass/retry/replan/partial/fail`？
4. `retry` 和 `replan` 的区别是什么？
5. 关键步骤失败和非关键步骤失败如何区别处理？
6. 如何防止 Evaluator 无限重试？
7. 如何防止 Planner 与 Evaluator 来回震荡？
8. Evaluator 本身判断错误怎么办？
9. Machine Validator 与 LLM Evaluator 有什么区别？
10. 最终答案如何保证只引用真实工具证据？
11. 工具返回空结果时，是成功还是失败？
12. 数据被截断时，系统能否直接声称“没有更多数据”？

**回答要点**

- HTTP 200 只代表调用完成，业务目标可能仍未满足，例如请求某交易却返回空结果。
- Evaluator 根据 PlanStep 成功标准和结构化结果决定后续动作。
- `retry` 通常保持步骤目标不变并修正执行；`replan` 表示原计划不再适用，需要调整步骤或工具。
- Step retry、replan 和全局工具调用都有硬上限；超过预算后返回 partial/fail，避免死循环。
- 最终 Composer 使用 StepResult、facts、provenance 和失败边界，而非仅依赖对话文本。

### 第四层：工具异常恢复

1. 什么错误属于瞬时故障？
2. timeout、429、503、参数错误、权限错误分别怎么处理？
3. 为什么参数错误不应该原参数机械重试？
4. 自动重试发生在哪一层？
5. 重试次数和退避时间如何配置？
6. 是否使用指数退避和 jitter？为什么需要 jitter？
7. 如何避免重试放大下游故障？
8. 工具不是幂等操作时还能自动重试吗？
9. 如何记录每次 attempt，便于排查故障？
10. “持续失败后参数修正”具体由哪个模块完成？
11. 参数修正后如何重新校验，避免模型继续猜参数？
12. 备用工具从哪里配置？切换前是否重新做权限判断？
13. 什么情况下应该切备用工具，什么情况下应该直接失败？
14. 主工具和备用工具的数据语义不一致怎么办？
15. 降级返回应该包含哪些信息？
16. 如果工具调用成功但响应 Schema 变化，如何分类？
17. 如果第一次请求已在下游成功、但本地超时，重试会不会造成重复副作用？

**回答要点**

- timeout、限流和服务暂不可用通常可在 ToolNode 层按原参数有限重试。
- 参数、Schema、权限错误不能机械重试；交由上层根据结构化错误做参数修正、改计划或降级。
- jitter 用于避免多实例在同一时刻形成重试风暴。
- 自动重试只应默认用于只读或明确幂等工具；副作用操作需要幂等键或更保守策略。
- fallback 不能绕过原有权限，也必须验证输入输出语义是否等价。

**高风险真实性追问**

> 你如何证明“参数修正、备用工具切换、重规划”真的端到端成功过？

回答时应区分：代码支持的状态迁移、组件测试覆盖、真实 HTTP E2E 验证。当前故障注入报告表明 timeout/429 等恢复主要在组件层验证，HTTP Evaluation Adapter 不支持把 fault injection 送入完整链路。不要把组件级证据包装成完整生产 E2E 结论。

### 第五层：权限、安全与恢复

1. 哪些工具需要用户确认？
2. 为什么权限判断不能完全交给 LLM？
3. 用户批准一个工具后，模型能否换另一个副作用工具？
4. 审批如何绑定步骤和工具？
5. 参数缺失时为什么不让模型自行补齐？
6. 澄清后如何从中断点继续，而不是重跑全部步骤？
7. 如何防止重跑前序副作用工具？
8. Checkpoint 中存了哪些恢复所需状态？
9. 多用户是否可能通过猜测 `thread_id` 操作别人的会话？
10. 生产环境还需要增加什么租户隔离措施？

**回答边界**

- 项目有确定性 Permission Gate，审批精确绑定 `step_id + tool_name`。
- Checkpoint 支持审批和补参后恢复。
- 不要声称已具备完善多租户安全体系；会话归属校验仍应作为重点改进项。

### 第六层：系统设计反问

1. 如果工具数量从 10 个增长到 200 个，架构如何演进？
2. 如果某个 RPC 节点 P95 延迟达到 20 秒，如何优化？
3. 哪些步骤可以并行执行？LangGraph 中怎么表达？
4. 如何做全链路 Trace，并关联一次请求里的多次工具尝试？
5. 如何设置全局预算：Token、工具次数、总耗时和费用？
6. 如何实现熔断、限流、隔离舱和 fallback 优先级？
7. 如果让你重构，你会把执行器拆成独立 Worker 吗？

---

## 三、上下文与记忆优化

简历原文：

> 针对长对话、大规模工具结果及跨线程历史信息导致的上下文膨胀问题，统一设计上下文预算与分层压缩机制，对工具结果提取关键事实、长会话滚动摘要，并按需召回长期记忆，平均 Token 消耗降低 32.1%。

### 第一层：概念辨析

1. Context、Checkpoint、Memory、Summary 分别是什么？
2. 为什么有大上下文窗口仍然需要压缩？
3. 长对话上下文主要由哪些内容构成？
4. 哪些信息绝对不能被裁剪？
5. 为什么不能简单保留最近 N 条消息？
6. 跨线程记忆为什么不能直接拼接全部历史对话？

**回答要点**

- Checkpoint 保存完整线程状态；Context 是某次模型调用实际看到的输入；Rolling Summary 压缩同线程旧消息；Long-term Memory 负责跨线程召回。
- 上下文过大不仅增加费用，也增加延迟、注意力稀释和关键信息遗漏风险。
- 系统约束、当前请求、已确认权限、关键实体、未解决错误和可靠证据优先级最高。

### 第二层：上下文预算

1. Token 预算如何计算？
2. 为什么要预留输出 Token？
3. 不同节点是否使用相同预算？
4. Router、Planner、Executor、Composer 各自需要什么上下文？
5. 超过预算时按什么优先级裁剪？
6. Token 估算与模型真实 tokenizer 不一致怎么办？
7. System Prompt 本身过大怎么办？
8. 如何在 Trace 中观测各类上下文占比？
9. 压缩后仍超限怎么办？
10. 模型返回 context-length error 时如何被动恢复？

**回答要点**

- 按场景构建上下文，而不是让每个节点看到完整历史。
- 输入上限应扣除预留输出和安全余量。
- ContextBuilder 按 system、summary constraints、current request、critical state、evidence、memory 等类别分配优先级并记录裁剪信息。
- 主动阈值压缩之外，还应在真实上下文超限后进行一次被动压缩重试，并限制次数。

### 第三层：工具结果压缩

1. 大型 SQL 查询结果为什么是主要 Token 消耗来源？
2. 工具结果达到什么条件触发压缩？
3. “提取关键事实”具体提取哪些字段？
4. 如何保证交易哈希、地址、金额等关键值不丢失？
5. 原始结果存在哪里？
6. 模型如何按需重新读取原始结果？
7. 为什么要保存 hash 和 `result_id`？
8. preview 截断后，模型是否可能得出错误的全局结论？
9. 聚合类问题应该在数据库计算还是把所有行交给模型？
10. 原始结果文件如何做权限隔离、生命周期管理和清理？
11. 如果结果包含敏感数据，写本地文件是否安全？

**回答要点**

- 大结果不直接进入消息，保留 preview、结构化 facts、hash、存储路径和 `result_id`。
- 原始结果与模型上下文分离，既节省 Token，也保留追溯能力。
- 截断结果不能用于证明全集结论；计数、聚合和排序应尽量下推数据库。
- 当前本地 Result Store 的权限、清理和多实例共享属于可继续完善的生产化问题。

### 第四层：Rolling Summary

1. 摘要在什么阈值触发？
2. 摘要覆盖到哪条消息，如何记录边界？
3. 如何避免把 AI tool call 与对应 ToolMessage 拆开？
4. 摘要应包含哪些结构化字段？
5. 摘要模型失败怎么办？
6. 摘要本身会不会越来越长？
7. 多次摘要是否会产生信息漂移？
8. 如何验证关键事实和约束仍然保留？
9. 为什么原始消息还留在 Checkpoint？
10. 30/50 轮测试验证了什么，没验证什么？

**回答要点**

- 只压缩安全历史前缀，不破坏工具调用配对。
- 摘要结构包含目标、用户约束、实体、数字、计划、已完成/待处理步骤、工具发现、错误、权限和开放问题。
- 完整消息仍在 Checkpoint 中，压缩的是送入模型的 active context。
- 当前确定性长对话测试证明机制和关键事实传递有效，但替换了真实 LLM，不能外推真实模型摘要的概率性保真率。

### 第五层：长期记忆召回

1. 什么信息适合写长期记忆？什么不适合？
2. 记忆是自动保存还是用户主动保存？
3. 为什么先做召回 Gate，再计算 Embedding？
4. 哪些措辞会触发“上次、之前、继续”的召回？
5. 相似度阈值和 Top-K 如何选择？
6. 如何保证只召回当前用户的记忆？
7. Memory 注入到 SystemMessage 还是普通消息？
8. 召回服务失败时主请求怎么办？
9. pgvector 不可用时如何降级？
10. 如何处理过期、冲突或错误记忆？
11. 用户要求删除记忆时如何彻底删除向量和原文？
12. 如何评估 recall precision 和 recall rate？

### 第六层：32.1% Token 降幅

这是本段最可能被深挖、也是当前证据风险最高的数字。

1. 32.1% 的计算公式是什么？
2. 分母是 input token、output token，还是 total token？
3. Baseline 是什么版本？只关闭压缩，还是同时关闭 Memory 和路由？
4. 测试用了多少条请求、多少轮对话、哪些任务类型？
5. 是否使用同一模型、同一 Prompt、同一数据和相同随机参数？
6. 是简单平均，还是按请求 Token 加权平均？
7. 降低 Token 后任务成功率有没有下降？
8. P50/P95 Token 降幅分别是多少？
9. 32.1% 是否只来自人工构造长对话，能否代表生产流量？
10. 工具原始结果落盘的存储和读取成本是否计入？
11. 为什么不是上下文越长降幅越大？
12. 能否现场指出实验报告和原始数据？

**当前仓库证据结论**

当前工作区没有检索到可复现“平均 Token 消耗降低 32.1%”的报告或原始对照数据。现有长对话报告证明 30/50 轮场景最终输入被控制在预算内，并证明关键事实保留，但这不等于相对基线降低 32.1%。面试前应二选一：

1. 补做严格 A/B：固定数据集、模型和参数，对比关闭/开启优化后的 `sum(input_tokens + output_tokens)`，同时报告成功率；
2. 暂时删除精确数字，改成“显著降低长对话与大结果场景的上下文 Token 消耗”。

---

## 四、链上交易监控与通知

简历原文：

> 实现地址和大额交易持续监控，基于 APScheduler 增量扫描交易数据，命中规则后自动发送飞书通知；通过唯一约束、失败重试和 PostgreSQL Advisory Lock，避免多实例下重复处理和重复通知。

### 第一层：业务流程

1. 用户如何创建地址监控和大额交易监控？
2. 规则支持哪些字段？
3. 规则何时开始生效？是否处理历史数据？
4. 用户如何配置自己的飞书 Webhook？
5. 一条新交易从进入数据库到飞书通知经历哪些步骤？
6. 规则是每条单独查询数据库，还是统一扫描后内存匹配？
7. 为什么选择后台扫描，而不是要求用户保持 HTTP 连接？

**回答要点**

- 规则支持地址、最小 Token 数量、最小 USD 金额、链、Token 和通知渠道。
- APScheduler 注册一个全局周期任务；每轮只扫描一次新交易，再和全部启用规则匹配，避免规则数等比例增加数据库扫描。
- 默认首次启动只记录当前最大游标，不发送历史通知；可配置处理已有数据。
- 命中先写通知事件，再读取 pending/failed 事件发送用户级飞书 Webhook。

### 第二层：增量扫描

1. 为什么用 keyset pagination，而不是 OFFSET？
2. 普通数据表的游标是什么？
3. 为什么 JustLend 使用 `(tx_seq, event_index)` 复合游标？
4. 如果同一交易有多个 event，仅使用 tx hash 会发生什么？
5. 游标存在哪里？重启后是否丢失？
6. 每批最多扫描多少条？积压超过 batch size 怎么办？
7. 扫描和游标更新是否在同一个事务？
8. 如果匹配事件写入成功但游标更新失败会怎样？
9. 如果游标更新成功但事件写入失败会怎样？
10. 使用 UUID 作为通用表游标是否可靠？
11. 发生迟到数据，ID 小于当前游标时怎么办？
12. 交易被更新而不是追加时能否检测？
13. 数据表清理或 ID 回绕后如何恢复？

**回答要点**

- Keyset pagination 在大表上通常比 OFFSET 稳定，复杂度不随页数线性恶化。
- JustLend 的复合游标可区分同一 `tx_seq` 下不同 `event_index`。
- 匹配事件和新游标在同一个 PostgreSQL 连接事务中持久化，异常时共同回滚。
- 方案依赖游标字段稳定、唯一且单调递增；迟到数据、回写更新和非有序 UUID 是现有方案边界。

### 第三层：规则匹配与性能

1. 地址是否统一大小写？TRON 和 Ethereum 地址规则是否相同？
2. 金额为何使用 Decimal，而不是 float？
3. 多个条件之间是 AND 还是 OR？
4. `amount` 和 `amount_usd` 缺失时如何判断？
5. 规则数量达到十万时，双重循环还能工作吗？
6. 如何按 chain、token、address 建索引或分桶？
7. 为什么不直接把匹配条件下推到 SQL？
8. 新规则是否应该回看之前已经扫描过的交易？
9. 规则执行顺序是否影响结果？

### 第四层：唯一约束与幂等

1. 唯一约束具体是哪两个字段？
2. 为什么是 `(rule_id, transaction_id)`，不是交易 hash？
3. 同一交易命中两个规则应该发几次？
4. 同一规则命中同一交易的两个事件应该发几次？
5. `ON CONFLICT DO NOTHING` 解决了什么竞态？
6. 唯一约束是在应用层还是数据库层？为什么数据库层更可靠？
7. 修改规则后，旧交易能否重新触发？
8. 删除再重建规则后，幂等语义是否改变？

**回答要点**

- 数据库唯一约束为 `(rule_id, transaction_id)`；事件插入使用 `ON CONFLICT DO NOTHING`。
- 它保证同一规则和交易只生成一条通知事件；不同规则可以分别通知。
- JustLend 的 `transaction_id` 实际由 `tx_seq:event_index` 组成，因此同一链上交易的多个业务事件不会被错误合并。

### 第五层：Advisory Lock 与多实例

1. 为什么 APScheduler 的 `max_instances=1` 不能解决多实例重复执行？
2. Advisory Lock 使用 session lock 还是 transaction lock？
3. 为什么使用 `pg_try_advisory_lock`，而不是阻塞式 lock？
4. 没抢到锁的实例怎么处理？
5. 锁覆盖扫描、匹配、写事件还是通知发送？
6. 进程崩溃后锁会不会永久残留？
7. 数据库连接断开后锁如何释放？
8. 锁 key 如何生成？`hashtext` 是否存在碰撞风险？
9. 多租户是否需要不同锁？
10. 如果监控库和交易库是两个 PostgreSQL 实例，锁保护的范围是什么？
11. 持锁期间飞书请求很慢会有什么影响？
12. 如果希望并行提高吞吐，如何做分片锁？

**回答要点**

- `max_instances=1` 只约束单进程内 Scheduler；每个应用副本都有自己的 Scheduler。
- Worker 使用非阻塞 `pg_try_advisory_lock`，未获得锁的副本跳过本轮。
- 当前 session lock 由持有连接维护，正常 finally 主动释放，连接断开时 PostgreSQL 也会释放。
- 当前锁覆盖扫描和通知全过程，简单但会受外部 Webhook 延迟影响；更高吞吐可拆分扫描入库与通知消费，并使用行级 claim/lease。

### 第六层：失败重试与投递语义

1. 通知事件有哪些状态？
2. 一次失败后什么时候重试？
3. 最多重试几次？
4. 是否有指数退避或 `next_retry_at`？
5. 永久失败后事件进入哪里？有没有死信队列？
6. 缺少用户 Webhook 属于可重试错误吗？
7. 飞书返回 HTTP 200 就一定发送成功吗？
8. 是否解析飞书响应 JSON 的业务错误码？
9. Webhook 超时但飞书实际收到消息时怎么办？
10. 飞书发送成功、数据库 `mark_sent` 前崩溃会怎样？
11. 当前系统是 at-most-once、at-least-once 还是 exactly-once？
12. 如何真正做到端到端 exactly-once？

**必须诚实回答的边界**

- 当前失败事件在后续扫描轮次继续重试，`attempts < 5`，但没有指数退避、错误分类或死信队列。
- 唯一约束保证通知事件不重复创建，却不能保证飞书只收到一次。
- 飞书成功与数据库 `sent` 状态是两个独立操作。如果飞书已收到、服务却在 `mark_sent` 前崩溃，下一轮会再次发送。因此当前更接近 **at-least-once**。
- Webhook 没有通用的跨系统原子事务；工程上通常依靠下游幂等键、可查询的发送状态、业务消息去重、Outbox + 消费者 claim 等机制降低重复。

### 第七层：推荐修正简历表述

当前“避免多实例下重复处理和重复通知”过于绝对。建议改为：

> 基于 APScheduler 和持久化游标增量扫描交易数据，规则命中后通过飞书 Webhook 自动告警；通过通知事件唯一约束、最多 5 次失败重试及 PostgreSQL Advisory Lock，实现多实例互斥扫描与事件幂等，降低重复处理和重复通知风险。

---

## 五、Agent 效果评估

简历原文：

> 搭建基于生产 Trace 的离线评测体系，覆盖任务成功、工具选择、参数准确性及权限等核心指标，用于验证 Agent 链路优化效果，最终任务成功率达到 90%，工具选择准确率达到 96.4%。

### 第一层：为什么需要 Agent Evaluation

1. 为什么普通单元测试不足以评估 Agent？
2. Agent 评测和传统接口测试有什么区别？
3. 离线评测解决什么问题，线上监控解决什么问题？
4. 为什么不能只看最终答案？
5. 为什么工具选择正确但任务仍可能失败？
6. 评测体系如何用于回归，而不只是生成一次报告？

### 第二层：数据集与 Ground Truth

1. “基于生产 Trace”具体如何抽样？
2. 是否对用户数据做脱敏？
3. 如何覆盖简单查询、多步任务、权限、恢复和 Memory？
4. Ground Truth 中保存哪些内容？
5. 谁标注正确工具和参数？如何处理标注分歧？
6. 数据集规模是多少？为什么足够？
7. 如何避免训练数据或 Prompt 调优对评测集过拟合？
8. 是否划分开发集、回归集和隐藏测试集？
9. 外部数据库数据变化后 Ground Truth 如何保持稳定？
10. skipped case 是否计入成功率？
11. 失败用例修复后是否保留在回归集？

**回答边界**

- 当前仓库数据集规模较小，适合作为工程回归集，不宜包装成统计意义充分的大规模生产 Benchmark。
- 数据集条数曾从 30 增至 34，而测试仍存在固定断言，说明数据集版本管理和 CI 一致性仍需加强。

### 第三层：指标定义

1. 任务成功率如何定义？partial 和 degraded 算成功吗？
2. 工具选择准确率是按请求、步骤还是工具调用计算？
3. 一个任务需要两个工具，只选对一个怎么算？
4. 多个工具都能完成任务时 Ground Truth 如何定义？
5. 参数准确率是字段级、调用级还是任务级？
6. 数值、地址、时间范围分别如何比较？
7. 默认参数和参数缺失怎么判定？
8. 权限准确率如何区分该放行和该拦截？
9. Recovery success rate 如何计算？
10. 如何报告延迟、Token 和模型/工具调用次数？
11. 为什么只报告平均值不够？
12. 如何按任务类别拆分指标，避免总体均值掩盖问题？

**回答要点**

- 仓库聚合逻辑把 outcome 为 `success` 的 case 计入 task success，partial/degraded/failed 分开统计。
- 工具选择、参数、恢复和权限来自确定性 check；性能从 Trace 中聚合 P50/P95 延迟及平均调用和 Token。
- 面试时需要明确 96.4% 的分母，不要只背百分比。

### 第四层：Trace 和评测执行

1. 一条 Trace 包含哪些事件？
2. 如何关联 node、tool、decision 和 error event？
3. Trace ID、thread ID、tool call ID、step ID 分别解决什么问题？
4. 如何从 Trace 判断发生了几次重试和重规划？
5. Trace 缺字段或旧版本格式变化怎么办？
6. Evaluation Adapter 的作用是什么？
7. HTTP Adapter 和组件 Adapter 有什么区别？
8. 为什么某些 fault-injection case 会被 skipped？
9. LLM Judge 在什么情况下启用？
10. 如何降低 LLM Judge 的随机性和自我偏好？
11. 评测失败如何定位到具体节点？

### 第五层：故障注入

1. 如何注入 timeout、429、参数错误和权限错误？
2. 故障注入发生在真实工具之前还是之后？
3. 如何验证 timeout 确实重试，而不是测试桩直接返回成功？
4. 如何记录 backoff delay 和底层真实调用次数？
5. 如何验证权限错误没有触发备用工具绕过？
6. 组件层通过为什么不等于 HTTP E2E 通过？
7. 如何把 fault injection 能力接入完整 Agent 请求链？
8. 如何在 CI 中保证故障测试稳定且执行时间可控？

### 第六层：90% 和 96.4% 的真实性审查

1. 90% 是哪次 run 的结果？报告文件在哪里？
2. 分母是多少？90% 对应多少条成功、多少条失败？
3. 是否排除了 skipped case？
4. 优化前成功率是多少？提升是否具有可比性？
5. 是否使用真实模型和真实外部工具？
6. 评测运行时数据库内容是否固定？
7. 为什么当前报告数字和简历数字不一致？
8. 96.4% 是 27/28 吗？具体失败的是哪一例？
9. 任务成功率低于工具选择准确率说明什么？
10. 是否可能选对工具但参数、外部依赖或最终回答失败？
11. 如果换模型或 Prompt，指标波动有多大？
12. 是否重复运行并报告置信区间？

**当前仓库可核验证据**

- `eval_results/run_20260823T124445Z.md` 中工具选择准确率为 **96.4%**。
- 同一次运行的任务成功率是 **78.6%（22/28）**，不是 90%。
- 当前仓库没有检索到能够支持“最终任务成功率达到 90%”的对应报告。
- 因此面试前必须找到 90% 对应的原始 run、数据集版本和计算口径；找不到就应把简历改成仓库可验证的 78.6%，或只描述“建立可回归的指标体系”，暂不写成功率。

### 第七层：评测体系如何继续完善

1. 如何扩大真实生产 Trace 覆盖并持续脱敏？
2. 如何建立每类任务的最低通过阈值？
3. 如何做 Prompt、模型和 Agent 架构的 A/B 对比？
4. 如何加入成本—质量 Pareto 指标？
5. 如何建立线上失败自动回流离线集的闭环？
6. 如何检测数据漂移、工具 Schema 漂移和模型版本漂移？
7. 如何避免 LLM Judge 成为唯一裁判？

---

## 六、跨模块综合深挖题

1. 为什么监控任务没有复用完整 Agent Planned 链路？
2. Scheduler、Monitoring Worker 和 Agent Executor 的可靠性要求有什么不同？
3. 如果 Agent 创建监控规则时重复执行，怎样保证规则本身不重复？当前是否有业务幂等键？
4. 如果 Memory 召回了过期规则，是否可能导致 Agent 创建错误监控？
5. 工具结果压缩后，Evaluator 判断步骤成功会不会缺少证据？
6. 如果备用 RPC 与主 RPC 返回冲突结果，Composer 应如何表达？
7. 如何把后台监控 Trace 纳入同一个离线评测体系？
8. 如何衡量上下文压缩对工具参数准确率的影响？
9. 如何验证 Direct/Planned 路由优化没有损害权限安全？
10. 多实例部署时，Checkpoint、Result Store、Scheduler 和 Monitoring 分别有哪些共享状态要求？
11. 如果从单机扩展到 Kubernetes，你首先改哪三个地方？
12. 哪些模块已经达到生产可用，哪些仍是工程原型？

**推荐回答方向**

- Monitoring 是确定性、高频后台任务，不需要每轮调用 LLM；规则创建可以经过 Agent，规则执行本身应保持确定性。
- 多实例下 PostgreSQL 状态可共享，本地 Result Store 和 Scheduler JSON 文件则需要迁移到共享或独立服务。
- 应把质量、安全、成本和可靠性一起评估，不能只优化单一成功率。

---

## 七、行为面试与个人贡献追问

1. 这个项目中最严重的一次线上或测试故障是什么？你如何定位？
2. 哪个方案最初设计错了，后来为什么重构？
3. 你如何说服团队采用 Direct/Planned 双路径？
4. 你与数据、前端或运维同学如何定义接口边界？
5. 如何决定先做上下文压缩而不是直接升级更大模型？
6. 你写了哪些测试？最有价值的一条测试是什么？
7. 如果项目延期，只保留三个功能，你保留什么？
8. 哪个指标是你亲自设计并验证的？
9. 如果重新做一次，你会避免什么技术债？
10. 你如何证明这些工作确实由你主导，而不是简单调用框架 API？

回答时应具体到一个决策、一段调用链、一个失败案例或一项数据。避免只说“负责 Agent 架构和性能优化”。

---

## 八、面试前必须准备的数据与材料

### 必须能当场说明

1. LangGraph 核心节点图和 Direct/Planned 的分流规则。
2. 一个数据库查询 → tx hash 提取 → TRON RPC 核验的完整例子。
3. timeout、429、参数错误和权限错误的不同恢复路径。
4. Context、Checkpoint、Rolling Summary、Memory 的区别。
5. 工具大结果压缩前后的实际 Token 示例。
6. 监控复合游标、唯一约束和 Advisory Lock 的作用范围。
7. 为什么通知只能做到 at-least-once，而不能声称严格 exactly-once。
8. 评测指标的分子、分母、数据集版本和失败案例。

### 当前需要补齐或修正

| 简历声明 | 当前证据 | 建议 |
|---|---|---|
| 平均 Token 消耗降低 32.1% | 未找到对应 A/B 报告 | 补充固定数据集的开关对照实验，否则删除精确数字 |
| 任务成功率达到 90% | 现存报告为 78.6% | 找到 90% 原始 run；否则改成可验证结果 |
| 工具选择准确率 96.4% | 现存报告支持 | 准备分母、失败 case 和计算口径 |
| 避免重复通知 | 只能保证事件幂等，飞书投递仍有重复窗口 | 改成“降低重复通知风险”或“实现事件幂等” |
| 基于生产 Trace | 仓库能证明 Trace 驱动框架，不能单独证明数据均来自真实生产 | 准备脱敏、采样和数据来源说明；否则改成“基于执行 Trace” |

---

## 九、推荐的最终简历版本

> **Agent 执行链路建设：**基于 LangGraph 搭建链上数据分析 Agent，接入 PostgreSQL、ClickHouse、链上 RPC 与可视化等工具；设计 Direct/Planned 双执行路径，并通过错误分类、有限退避重试、参数修正、备用工具与重规划/降级机制，提高多工具任务的可恢复性。  
> **上下文与记忆优化：**面向长对话、大规模工具结果和跨线程历史，设计分场景 Token 预算与分层压缩机制；对工具结果提取结构化事实并保留原始结果引用，通过滚动摘要与按需长期记忆召回控制上下文规模。  
> **链上交易监控与通知：**基于 APScheduler 和持久化游标增量扫描地址及大额交易，命中规则后通过飞书 Webhook 告警；结合通知事件唯一约束、最多 5 次失败重试与 PostgreSQL Advisory Lock，实现多实例互斥扫描和事件幂等，降低重复通知风险。  
> **Agent 效果评估：**搭建基于执行 Trace 的离线评测体系，覆盖任务成功、工具选择、参数、恢复和权限指标，并输出延迟、Token 与调用次数报告；当前可复现评测中工具选择准确率达到 96.4%。

精确的 Token 降幅和任务成功率应在补齐可复现实验后再写回简历。

